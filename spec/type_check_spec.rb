require "spec_helper"

describe TypeCheck do
  it "has a version number" do
    expect(TypeCheck::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
